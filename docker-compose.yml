services:
  bot:
    build: .
    # env_file: .env  # Removed - configuration via admin panel
    ports:
      - "8000:8000"
    volumes:
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data:
