# План работ: экран «Основные настройки приложения»

Декомпозиция PRD «Main app settings screen» в отдельные задачи. Работы не начинать до согласования плана.

---

## Ссылки

- **PRD:** [PRD - main app settings screen.md](../PRD%20-%20main%20app%20settings%20screen.md)
- **Текущая реализация:** [docs/CURRENT_IMPLEMENTATION.md](CURRENT_IMPLEMENTATION.md)

---

## Фаза 0: Подготовка (выполнено)

- [x] Зафиксировать текущую реализацию в `docs/CURRENT_IMPLEMENTATION.md`
- [x] Создать ветку для работ (например `feature/main-app-settings`)

---

## Фаза 1: Бэкенд — хранилище и API настроек

### 1.1 Хранилище настроек

- **Задача:** Ввести БД (или иное хранилище) для настроек Telegram и LLM.
- **Детали:**
  - Таблицы/модели: `telegram_settings`, `llm_settings` (или один модуль «settings» с двумя блоками).
  - Поля по PRD: accessToken/baseUrl, connectionStatus, isActive, lastActivatedAt (Telegram); llmType, apiKey, baseUrl, modelType, systemPrompt, connectionStatus, isActive, lastActivatedAt (LLM).
  - API-ключи и токены хранить в зашифрованном виде.
- **Результат:** Миграции/схема, загрузка и сохранение настроек из кода.

### 1.2 API: получение всех настроек

- **Задача:** Реализовать `GET /api/settings`.
- **Детали:** Возвращать оба блока (Telegram, LLM). Секреты — в маскированном виде (последние 5 символов).
- **Результат:** Эндпоинт, тесты.

### 1.3 API: сохранение настроек Telegram

- **Задача:** Реализовать `PUT /api/settings/telegram`.
- **Детали:** Валидация (Access Token обязателен, Base URL по умолчанию если пусто). Сохранить только блок Telegram. После сохранения — вызвать проверку соединения; при успехе — пометить как активные и применить к приложению (см. 1.7).
- **Результат:** Эндпоинт, валидация, запись в БД.

### 1.4 API: проверка соединения Telegram

- **Задача:** Реализовать `POST /api/settings/telegram/test`.
- **Детали:** Запрос `GET {Base URL}/bot{Access Token}/getMe`. Возвращать статус (success / failed) и при необходимости краткое описание ошибки.
- **Результат:** Эндпоинт, тесты.

### 1.5 API: применение настроек Telegram к приложению

- **Задача:** Реализовать `POST /api/settings/telegram/activate` (или объединить с `/test`: при успешной проверке — автоматическая активация).
- **Детали:** Пометить настройки как активные, обновить `lastActivatedAt`. Логика «переключения бота на лету» — в задаче 1.8.
- **Результат:** Эндпоинт активации и/или объединённая логика test+activate.

### 1.6 API: сохранение настроек LLM

- **Задача:** Реализовать `PUT /api/settings/llm`.
- **Детали:** Валидация (LLM type, API key, Model type обязательны; Base URL по умолчанию для выбранного провайдера если пусто). Сохранить только блок LLM. После сохранения — проверка соединения; при успехе — активация (см. 1.9).
- **Результат:** Эндпоинт, валидация, запись в БД.

### 1.7 API: проверка соединения LLM

- **Задача:** Реализовать `POST /api/settings/llm/test`.
- **Детали:** В зависимости от провайдера: например OpenAI — `GET /models` или минимальный completion; Anthropic — тестовый запрос к messages API. Возвращать success/failed и при необходимости расшифровку (Invalid API key, Rate limit и т.д.).
- **Результат:** Эндпоинт, тесты под разные провайдеры.

### 1.8 API: применение настроек LLM к приложению

- **Задача:** Реализовать `POST /api/settings/llm/activate` (или объединить с `/test`).
- **Детали:** Пометить настройки LLM как активные. Логика «переключения LLM на лету» — в задаче 1.10.
- **Результат:** Эндпоинт активации и/или объединённая логика test+activate.

### 1.9 API: список провайдеров и моделей

- **Задача:** Реализовать `GET /api/settings/llm/providers`.
- **Детали:** Возвращать список провайдеров (LLM type) и для каждого — Base URL по умолчанию, списки моделей: стандартные и reasoning (по PRD). Нужно для заполнения dropdown на фронте.
- **Результат:** Эндпоинт, структура данных по разделам 4.4–4.5 PRD.

### 1.10 Переключение бота на лету

- **Задача:** Подключить работающий бот к настройкам Telegram из БД (активные настройки с `is_active=true`).
- **Детали:** При активации новых настроек Telegram: корректно завершить соединение со старым ботом, запустить polling с новым токеном/Base URL. Учесть single-instance и конфликты getUpdates.
- **Результат:** После успешного сохранения и проверки Telegram приложение переходит на нового бота без перезапуска процесса.

### 1.11 Переключение LLM на лету

- **Задача:** Подключить логику LLM к настройкам из БД (активные настройки с `is_active=true`).
- **Детали:** При активации новых настроек LLM: использовать выбранный провайдер, API key, Base URL, модель, системный промпт из БД. Без перезапуска приложения; при отсутствии активных настроек — fallback на текущее поведение из .env (опционально).
- **Результат:** После успешного сохранения и проверки LLM все новые запросы идут через новые настройки.

### 1.12 Авторизация и аудит

- **Задача:** Ограничить доступ к API настроек только пользователям с ролью `admin`. Логировать все изменения настроек (кто, когда, что изменил).
- **Детали:** Выбор способа авторизации (JWT, сессии, и т.д.) и хранение ролей — отдельно. Минимум: проверка роли перед обработкой PUT/POST настроек; логирование изменений.
- **Результат:** Доступ только для админов, записи в лог/аудит при изменении настроек.

---

## Фаза 2: Фронтенд — макет и блок «Телеграм бот»

### 2.1 Макет страницы

- **Задача:** Реализовать общий макет админ-панели по PRD и макету из дизайна.
- **Детали:** Левая панель: заголовок «Admin panel», пункт меню «Настройки» (активный). Основная область: заголовок «Основные настройки приложения», две колонки (Телеграм бот | LLM). На экранах &lt; 1024px — одна колонка (вертикально). Минимальная ширина контента 320px.
- **Результат:** Страница с пустыми блоками или заголовками блоков.

### 2.2 Блок «Телеграм бот» — UI

- **Задача:** Реализовать элементы блока «Телеграм бот».
- **Детали:** Статус-бар (цвета по PRD: успех #d4edda, ошибка #f8d7da, не настроено #e9ecef, проверка #cce5ff). Кнопка «Retry». Поля: Access Token * (password), Base URL (text). Подсказка под Base URL: «По умолчанию: https://api.telegram.org». Кнопка «Сохранить» (primary).
- **Результат:** Внешний вид блока соответствует PRD и макету.

### 2.3 Блок «Телеграм бот» — загрузка и автопроверка

- **Задача:** При загрузке страницы загружать настройки Telegram из `GET /api/settings`; маскировать токен (последние 5 символов); при пустом Base URL подставлять значение по умолчанию; при заполненном токене запускать автопроверку каждые 10 секунд.
- **Результат:** Корректное отображение сохранённых данных и периодическое обновление статуса.

### 2.4 Блок «Телеграм бот» — Retry и Сохранить

- **Задача:** Кнопка «Retry» — немедленный вызов `POST /api/settings/telegram/test`, во время запроса показывать «Checking connection...». Кнопка «Сохранить» — валидация, `PUT /api/settings/telegram`, затем проверка соединения; при успехе — toast «Настройки сохранены и применены», при ошибке — «Настройки сохранены, но не применены (ошибка подключения)».
- **Результат:** Retry и сохранение работают по сценариям PRD.

---

## Фаза 3: Фронтенд — блок «LLM»

### 3.1 Блок «LLM» — UI

- **Задача:** Реализовать элементы блока «LLM».
- **Детали:** Статус-бар, кнопка «Retry». Поля: LLM type * (dropdown), API key * (password), Base URL (text), Model type * (dropdown с группами «Стандартные модели» и «Reasoning модели (🧠)»), System prompt (textarea, optional). Подсказки под Base URL по провайдеру. Tooltip для reasoning-моделей. Кнопка «Сохранить».
- **Результат:** Внешний вид и структура dropdown моделей по PRD 4.5.

### 3.2 Блок «LLM» — данные и провайдеры

- **Задача:** Загружать список провайдеров и моделей из `GET /api/settings/llm/providers`. При загрузке страницы подставлять сохранённые настройки LLM (API key маскирован). При смене LLM type — подставлять Base URL по умолчанию, обновлять список моделей, сбрасывать выбранную модель (или выбирать первую).
- **Результат:** Dropdown провайдеров и моделей заполняются из API; поведение при смене провайдера по PRD.

### 3.3 Блок «LLM» — автопроверка, Retry, Сохранить

- **Задача:** Автопроверка статуса каждые 10 секунд при заполненном API key. «Retry» — немедленный `POST /api/settings/llm/test` с отображением «Checking connection...». «Сохранить» — валидация, `PUT /api/settings/llm`, проверка соединения, toast по результату (аналогично блоку Telegram).
- **Результат:** Логика блока LLM соответствует PRD.

---

## Фаза 4: Общее UX и безопасность

### 4.1 Состояния кнопок и полей

- **Задача:** Реализовать состояния UI по PRD 5.5: кнопки — Default, Loading (спиннер при сохранении/проверке), Disabled (если форма невалидна); поля — Default, Error (красная обводка + сообщение), Disabled.
- **Результат:** Единообразное поведение кнопок и полей.

### 4.2 Toast-уведомления

- **Задача:** Реализовать toast по PRD 5.6: успех (настройки сохранены и применены), предупреждение (сохранены, но не применены), ошибка сохранения, ошибка валидации («Заполните обязательные поля»).
- **Результат:** Все сценарии сохранения отображают нужный тип и текст.

### 4.3 Маскирование секретов на фронте

- **Задача:** Никогда не показывать полные токены/ключи; отображать только маскированную версию (например последние 5 символов). Не логировать полные значения на клиенте.
- **Результат:** Соответствие требованиям безопасности PRD 5.4.

---

## Фаза 5: Приёмка и документация

### 5.1 Acceptance criteria

- **Задача:** Пройти по чек-листам PRD раздел 7 (блок Телеграм бот, блок LLM, применение настроек, общее). Исправить расхождения.
- **Результат:** Все пункты 7.1–7.4 отмечены как выполненные.

### 5.2 Документация и деплой

- **Задача:** Обновить README и при необходимости `docs/CURRENT_IMPLEMENTATION.md`: как запускать админ-панель, как настроить авторизацию админа, описание API настроек. При необходимости — инструкция по деплою (в т.ч. миграции БД).
- **Результат:** Документация позволяет развернуть и пользоваться экраном настроек.

---

## Порядок выполнения (рекомендуемый)

1. **Фаза 1** (бэкенд): 1.1 → 1.2 → 1.4 → 1.3 → 1.5 → 1.7 → 1.6 → 1.8 → 1.9 → 1.10 → 1.11 → 1.12.  
   Задачи 1.10 и 1.11 можно делать после появления первого рабочего API, чтобы сразу подключать «горячее» переключение.
2. **Фаза 2** (фронт Telegram): 2.1 → 2.2 → 2.3 → 2.4.
3. **Фаза 3** (фронт LLM): 3.1 → 3.2 → 3.3.
4. **Фаза 4** (UX): 4.1, 4.2, 4.3 — параллельно или по мере необходимости.
5. **Фаза 5**: 5.1 → 5.2.

---

## Риски и зависимости

- **Переключение бота на лету (1.10):** возможны ограничения со стороны `python-telegram-bot` (один процесс — один polling). Может потребоваться перезапуск потока/процесса обработки обновлений или отдельный механизм переподключения.
- **Фронтенд:** в PRD не зафиксирован стек (React, Vue, и т.д.). Выбор стека и инициализация проекта фронта — отдельное решение перед фазами 2–3.
- **Авторизация (1.12):** если сейчас нет системы пользователей и ролей, потребуется минимальная реализация «admin» (например, один захардкоженный пользователь или конфиг) или отложить до введения полноценной авторизации.

---

## Версионирование плана

| Версия | Дата       | Описание                |
|--------|------------|-------------------------|
| 1.0    | 2026-02-03 | Первая декомпозиция PRD |
