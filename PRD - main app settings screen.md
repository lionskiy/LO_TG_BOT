# PRD: Экран "Основные настройки приложения"

## 1. Общее описание

### 1.1 Назначение
Административная панель для настройки интеграций приложения с Telegram Bot API и LLM-провайдерами. Экран позволяет администратору настроить подключения и видеть их актуальный статус в реальном времени.

### 1.2 Целевая аудитория
Администраторы системы с соответствующими правами доступа.

---

## 2. Структура интерфейса

### 2.1 Общий макет

**Левая панель (сайдбар):**
- Заголовок: "Admin panel"
- Пункт меню: "Настройки" (активный, выделен акцентным цветом)

**Основная область:**
- Заголовок страницы: "Основные настройки приложения"
- Два функциональных блока в две колонки:
  - Левая колонка: "Телеграм бот"
  - Правая колонка: "LLM" (включает настройки подключения и системный промпт)

---

## 3. Блок "Телеграм бот"

### 3.1 Назначение
Настройка подключения к Telegram Bot API.

### 3.2 Элементы интерфейса

| Элемент | Тип | Обязательность | Описание |
|---------|-----|----------------|----------|
| Статус-бар | Индикатор | — | Отображает текущий статус подключения |
| Кнопка "Retry" | Button | — | Принудительная проверка соединения |
| Access Token * | Input (password) | Обязательное | Токен бота Telegram |
| Base URL | Input (text) | Опциональное | URL API Telegram |
| Кнопка "Сохранить" | Button (primary) | — | Сохранение настроек блока |

### 3.3 Статусы подключения

| Статус | Цвет | Текст | Условие |
|--------|------|-------|---------|
| Успех | Зелёный (#d4edda) | "Connection tested successfully" | Успешный ответ от API |
| Ошибка | Красный (#f8d7da) | "Connection failed" | Ошибка при запросе к API |
| Не настроено | Серый (#e9ecef) | "Not configured" | Токен не заполнен |
| Проверка | Синий (#cce5ff) | "Checking connection..." | Идёт проверка |

### 3.4 Логика работы

#### При загрузке страницы:
1. Загрузить сохранённые значения из БД
2. Если данные есть — отобразить токен в маскированном виде (показывать только последние 5 символов)
3. Если данных нет — оставить поля пустыми, отобразить статус "Not configured"
4. Если Base URL пустой — подставить значение по умолчанию: `https://api.telegram.org`
5. Запустить автоматическую проверку статуса (если токен заполнен)

#### Автоматическая проверка статуса:
- Выполняется каждые **10 секунд** (если токен заполнен)
- Запрос: `GET {Base URL}/bot{Access Token}/getMe`
- При успехе (HTTP 200 + `ok: true`) — зелёный статус
- При ошибке — красный статус
- Проверка не выполняется, если токен пустой

#### При нажатии "Retry":
1. Немедленно выполнить проверку соединения (сбросить таймер автопроверки)
2. Показать статус "Checking connection..." во время запроса
3. Отобразить результат

#### При изменении токена:
- Если токен был пустым и стал заполненным — автоматически запустить проверку после сохранения

#### При нажатии "Сохранить":
1. Валидация:
   - Access Token — обязательное поле
   - Base URL — если пустой, подставить значение по умолчанию
2. Сохранить **только данные этого блока** в БД
3. Автоматически выполнить проверку соединения
4. Показать toast-уведомление об успехе сохранения

#### Поле Base URL:
- Значение по умолчанию: `https://api.telegram.org`
- Если пользователь изменил значение — сохранить введённое
- Если поле пустое при сохранении — автоматически подставить значение по умолчанию
- Подсказка под полем: "По умолчанию: https://api.telegram.org"

---

## 4. Блок "LLM"

### 4.1 Назначение
Настройка подключения к LLM-провайдеру и системного промпта.

### 4.2 Элементы интерфейса

| Элемент | Тип | Обязательность | Описание |
|---------|-----|----------------|----------|
| Статус-бар | Индикатор | — | Отображает текущий статус подключения |
| Кнопка "Retry" | Button | — | Принудительная проверка соединения |
| LLM type * | Dropdown | Обязательное | Выбор провайдера |
| API key * | Input (password) | Обязательное | Ключ API провайдера |
| Base URL | Input (text) | Опциональное | Эндпоинт API |
| Model type * | Dropdown | Обязательное | Выбор модели |
| System prompt | Textarea | Опциональное | Системный промпт для LLM |
| Кнопка "Сохранить" | Button (primary) | — | Сохранение настроек блока |

### 4.3 Статусы подключения

Аналогично блоку "Телеграм бот" (см. раздел 3.3).

### 4.4 Доступные провайдеры и модели

| LLM type | Base URL по умолчанию | Стандартные модели | Reasoning модели (думающие) |
|----------|----------------------|--------------------|-----------------------------|
| OpenAI | `https://api.openai.com/v1` | gpt-4o, gpt-4.1, gpt-4.1-mini, gpt-4.1-nano | o3, o3-mini, o3-pro, o4-mini |
| Anthropic | `https://api.anthropic.com/v1` | claude-3-opus, claude-3-sonnet, claude-3-haiku, claude-3.5-sonnet | claude-3.5-sonnet (extended thinking) |
| Google Gemini | `https://generativelanguage.googleapis.com/v1beta/openai/` | gemini-2.5-flash, gemini-2.0-flash, gemini-3-flash | gemini-2.5-pro, gemini-3-pro, gemini-3-deep-think |
| Google Nano Banana | `https://generativelanguage.googleapis.com/v1beta/openai/` | gemini-2.5-flash-image, gemini-3-pro-image | — |
| xAI (Grok) | `https://api.x.ai/v1` | grok-3, grok-2 | grok-4, grok-4-reasoning |
| Perplexity | `https://api.perplexity.ai` | sonar, sonar-pro | sonar-reasoning, sonar-reasoning-pro, sonar-deep-research |
| DeepSeek | `https://api.deepseek.com` | deepseek-chat, deepseek-coder | deepseek-reasoner (R1) |
| Custom | — (ручной ввод) | Ручной ввод | Ручной ввод |

> **Примечание:** 
> - **Reasoning модели (думающие)** — модели с расширенными возможностями рассуждения, которые "думают" перед ответом. Обычно медленнее, но точнее на сложных задачах.
> - **Google Nano Banana** — неофициальное название моделей генерации изображений Gemini.
> - Все API совместимы с OpenAI SDK форматом.

### 4.5 UI для выбора модели

В dropdown "Model type" модели должны быть визуально разделены на группы:

```
┌─────────────────────────────────┐
│ Model type *                  ▼ │
├─────────────────────────────────┤
│ ── Стандартные модели ────────  │
│   gpt-4o                        │
│   gpt-4.1                       │
│   gpt-4.1-mini                  │
│   gpt-4.1-nano                  │
│ ── Reasoning модели (🧠) ─────  │
│   o3                            │
│   o3-mini                       │
│   o3-pro                        │
│   o4-mini                       │
└─────────────────────────────────┘
```

**Требования к UI:**
- Группы разделены визуальным заголовком (disabled option или разделитель)
- Reasoning модели помечены иконкой 🧠 или badge "Reasoning"
- При наведении на reasoning модель — tooltip: "Думающая модель: дольше отвечает, но точнее на сложных задачах"
- Порядок: сначала стандартные, затем reasoning

### 4.5 Логика работы

#### При загрузке страницы:
1. Загрузить сохранённые значения из БД
2. Если данные есть — отобразить, API key в маскированном виде
3. Если данных нет — оставить поля пустыми, отобразить статус "Not configured"
4. Запустить автоматическую проверку статуса (если API key заполнен)

#### Автоматическая проверка статуса:
- Выполняется каждые **10 секунд** (если API key заполнен)
- Метод проверки зависит от провайдера:
  - Open AI: `GET /models` или минимальный completion-запрос
  - Anthropic: тестовый запрос к messages API
- При успехе — зелёный статус
- При ошибке — красный статус с расшифровкой (Invalid API key, Rate limit exceeded, и т.д.)

#### При смене LLM type:
1. Автоматически подставить Base URL по умолчанию для выбранного провайдера
2. Обновить список доступных моделей в dropdown "Model type"
3. Сбросить выбранную модель (или выбрать первую из списка)

#### Поле Base URL:
- Автоматически заполняется при выборе провайдера
- Если пользователь изменил значение — сохранить введённое
- Если поле пустое при сохранении — подставить значение по умолчанию для выбранного провайдера
- Подсказка под полем: "По умолчанию для {Provider}: {URL}"

#### Поле System prompt:
- Опциональное поле
- Placeholder: "Введите системный промпт для настройки поведения модели..."
- Если пустое — LLM будет использоваться без системного промпта (или с дефолтным на бэкенде)

#### При нажатии "Сохранить":
1. Валидация:
   - LLM type — обязательное
   - API key — обязательное
   - Model type — обязательное
   - Base URL — если пустой, подставить значение по умолчанию
   - System prompt — опционально (может быть пустым)
2. Сохранить **только данные этого блока** в БД
3. Автоматически выполнить проверку соединения
4. Показать toast-уведомление об успехе сохранения

---

## 5. Общие требования

### 5.1 Независимость блоков
- Каждый блок сохраняется **независимо** от другого
- Кнопка "Сохранить" в блоке "Телеграм бот" сохраняет только настройки Telegram
- Кнопка "Сохранить" в блоке "LLM" сохраняет только настройки LLM и промпт

### 5.2 Применение настроек к работе приложения

**Критически важно:** После успешного сохранения и прохождения проверки соединения (статус "Connection tested successfully") настройки должны **немедленно применяться** к работе приложения.

#### Telegram бот:
- После успешной проверки соединения приложение должно **переключиться** на использование указанного бота
- Все новые входящие сообщения обрабатываются через нового бота
- Все исходящие сообщения отправляются через нового бота
- Если бот был изменён — корректно завершить соединение со старым ботом и установить соединение с новым

#### LLM:
- После успешной проверки соединения приложение должно **переключиться** на использование указанной модели
- Все новые запросы к LLM используют:
  - Выбранного провайдера (LLM type)
  - Указанный API key
  - Указанный Base URL
  - Выбранную модель (Model type)
  - Системный промпт (если заполнен)
- Переключение происходит "на лету" без необходимости перезапуска приложения

#### Логика применения:

```
┌─────────────────────────────────────────────────────────────────┐
│  Пользователь нажимает "Сохранить"                              │
└─────────────────────┬───────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│  Валидация полей                                                │
│  (если ошибка → показать ошибку, не сохранять)                  │
└─────────────────────┬───────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│  Сохранение в БД                                                │
└─────────────────────┬───────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│  Автоматическая проверка соединения                             │
└─────────────────────┬───────────────────────────────────────────┘
                      ▼
            ┌─────────┴─────────┐
            ▼                   ▼
┌───────────────────┐  ┌────────────────────────────────────────┐
│  Connection       │  │  Connection tested successfully        │
│  failed           │  │                                        │
│                   │  │  → Применить настройки к приложению    │
│  → Показать       │  │  → Показать toast "Настройки           │
│    ошибку         │  │    сохранены и применены"              │
│  → Настройки      │  │  → Приложение использует новые         │
│    сохранены,     │  │    настройки немедленно                │
│    но НЕ          │  │                                        │
│    применены      │  │                                        │
└───────────────────┘  └────────────────────────────────────────┘
```

#### Важные сценарии:

| Сценарий | Поведение |
|----------|-----------|
| Новые настройки, проверка успешна | Сохранить + применить немедленно |
| Новые настройки, проверка неуспешна | Сохранить в БД, но НЕ применять. Приложение продолжает работать со старыми настройками (если были) |
| Изменение настроек, проверка успешна | Применить новые настройки, заменив старые |
| Изменение настроек, проверка неуспешна | Сохранить в БД, продолжать работать со старыми применёнными настройками |

#### Флаг активности настроек:

В БД для каждого блока настроек хранить флаг `is_active`:
- `true` — настройки прошли проверку и применены к приложению
- `false` — настройки сохранены, но не применены (проверка не пройдена)

```typescript
interface TelegramSettings {
  // ... остальные поля
  isActive: boolean;        // применены ли настройки к приложению
  lastActivatedAt: DateTime | null;
}

interface LLMSettings {
  // ... остальные поля
  isActive: boolean;        // применены ли настройки к приложению
  lastActivatedAt: DateTime | null;
}
```

### 5.3 API Endpoints

```
GET    /api/settings                    — получить все настройки
PUT    /api/settings/telegram           — сохранить настройки Telegram
POST   /api/settings/telegram/test      — проверить соединение с Telegram
POST   /api/settings/telegram/activate  — применить настройки Telegram к приложению
PUT    /api/settings/llm                — сохранить настройки LLM (включая промпт)
POST   /api/settings/llm/test           — проверить соединение с LLM
POST   /api/settings/llm/activate       — применить настройки LLM к приложению
GET    /api/settings/llm/providers      — список провайдеров и их моделей
```

> **Примечание:** Эндпоинты `/activate` могут быть объединены с `/test` — если проверка успешна, настройки автоматически активируются.

### 5.4 Безопасность

- API-ключи и токены хранить в зашифрованном виде в БД
- При передаче на фронтенд — только маскированная версия (последние 5 символов)
- Для изменения ключа — отдельная логика (полностью перезаписывать поле)
- Доступ к странице только для пользователей с ролью `admin`
- Логирование всех изменений настроек (кто, когда, что изменил)

### 5.5 Состояния UI-элементов

**Кнопки:**
| Состояние | Описание |
|-----------|----------|
| Default | Активная, кликабельна |
| Loading | Спиннер, некликабельна (при сохранении/проверке) |
| Disabled | Серая, если форма невалидна |

**Поля ввода:**
| Состояние | Описание |
|-----------|----------|
| Default | Обычное состояние |
| Error | Красная обводка + сообщение об ошибке под полем |
| Disabled | Серое, недоступно для редактирования |

### 5.6 Уведомления (Toast)

| Событие | Тип | Текст |
|---------|-----|-------|
| Успешное сохранение и применение | Success | "Настройки сохранены и применены" |
| Сохранено, но не применено | Warning | "Настройки сохранены, но не применены (ошибка подключения)" |
| Ошибка сохранения | Error | "Ошибка сохранения: {описание}" |
| Ошибка валидации | Warning | "Заполните обязательные поля" |

### 5.7 Responsive Design

- На экранах < 1024px блоки располагаются в одну колонку (вертикально)
- Минимальная ширина контента: 320px

---

## 6. Технические детали

### 6.1 Polling статуса

```javascript
// Псевдокод автопроверки
const STATUS_CHECK_INTERVAL = 10000; // 10 секунд

function startStatusPolling(service) {
  if (!hasCredentials(service)) {
    setStatus(service, 'not_configured');
    return;
  }
  
  checkConnection(service);
  
  setInterval(() => {
    if (hasCredentials(service)) {
      checkConnection(service);
    }
  }, STATUS_CHECK_INTERVAL);
}
```

### 6.2 Структура данных

```typescript
interface TelegramSettings {
  accessToken: string;          // encrypted in DB
  baseUrl: string;              // default: "https://api.telegram.org"
  lastChecked: DateTime;
  connectionStatus: 'success' | 'failed' | 'not_configured';
  isActive: boolean;            // применены ли настройки к приложению
  lastActivatedAt: DateTime | null;
}

interface LLMSettings {
  llmType: 'openai' | 'anthropic' | 'custom';
  apiKey: string;               // encrypted in DB
  baseUrl: string;              // depends on llmType
  modelType: string;
  systemPrompt: string | null;
  lastChecked: DateTime;
  connectionStatus: 'success' | 'failed' | 'not_configured';
  isActive: boolean;            // применены ли настройки к приложению
  lastActivatedAt: DateTime | null;
}
```

---

## 7. Acceptance Criteria

### 7.1 Блок "Телеграм бот"
- [ ] При первом входе поля пустые, статус "Not configured"
- [ ] При заполнении токена и сохранении — автоматическая проверка подключения
- [ ] Статус обновляется каждые 10 секунд (если токен заполнен)
- [ ] Кнопка "Retry" немедленно проверяет соединение
- [ ] Base URL подставляется по умолчанию, если пустой
- [ ] Сохраняются только настройки этого блока
- [ ] **При успешной проверке — настройки немедленно применяются к приложению**
- [ ] **Приложение начинает использовать нового бота без перезапуска**

### 7.2 Блок "LLM"
- [ ] При первом входе поля пустые, статус "Not configured"
- [ ] При смене провайдера — автозаполнение Base URL и обновление списка моделей
- [ ] При заполнении API key и сохранении — автоматическая проверка подключения
- [ ] Статус обновляется каждые 10 секунд (если API key заполнен)
- [ ] System prompt опционален, работает с placeholder
- [ ] Сохраняются только настройки этого блока
- [ ] **При успешной проверке — настройки немедленно применяются к приложению**
- [ ] **Приложение начинает использовать новую модель/провайдера без перезапуска**

### 7.3 Применение настроек
- [ ] Настройки применяются только после успешной проверки соединения
- [ ] При неуспешной проверке — настройки сохранены в БД, но не применены
- [ ] Приложение продолжает работать со старыми настройками при ошибке подключения новых
- [ ] Toast-уведомление чётко указывает, применены настройки или только сохранены

### 7.4 Общее
- [ ] Токены/ключи отображаются в маскированном виде
- [ ] Валидация обязательных полей перед сохранением
- [ ] Toast-уведомления при сохранении
- [ ] Страница доступна только админам

---

## 8. Версионирование документа

| Версия | Дата | Автор | Изменения |
|--------|------|-------|-----------|
| 1.0 | 2026-02-03 | — | Первоначальная версия |
